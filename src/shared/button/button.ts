import { styled } from '@mui/system';
import { Button } from '@mui/material';

export const StyledButton = styled(Button)(() => ({
  marginRight: 10,
  textTransform: 'none',
  borderRadius: 0,
  height: 28,
  width: 134,
}));
